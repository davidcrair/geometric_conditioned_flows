# @package _global_

defaults:
  - override /data: sciplex
  - override /splitter: strict_k562
  - override /space: log1p_deg
  - override /condition: sciplex
  - override /task: baseline
  - override /model: baseline_no_effect
  - override /loss: baseline
  - override /callbacks: baseline

experiment_name: sciplex_deg_baseline_no_effect
