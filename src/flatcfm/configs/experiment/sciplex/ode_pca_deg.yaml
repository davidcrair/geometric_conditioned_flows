# @package _global_

defaults:
  - override /data: sciplex
  - override /splitter: strict_k562
  - override /space: pca_deg
  - override /condition: sciplex
  - override /task: ode
  - override /model: ode
  - override /loss: ode

experiment_name: sciplex_ode_deg_pca
