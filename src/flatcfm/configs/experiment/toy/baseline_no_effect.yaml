# @package _global_

defaults:
  - override /data: toy
  - override /splitter: toy
  - override /space: identity
  - override /condition: toy
  - override /task: baseline
  - override /model: baseline_no_effect
  - override /loss: baseline
  - override /callbacks: baseline

experiment_name: toy_baseline_no_effect
